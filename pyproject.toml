[project]
name = "hdmi-matrix-ctrl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyinstaller>=6.15.0",
    "pyqt5>=5.15.11; sys_platform != 'win32'",
    "requests>=2.32.4",
]
